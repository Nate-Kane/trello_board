<h4>Change task</h4>
 <!-- later add in more styling to these. Do a div class="forms" maybe -->
<%= form_with(model: [@board, @list], local: true) do |f| %>
  <div class="field">
    <%= f.label :task %>
    <%= f.text_field :task, required: true, placeholder: "#{@list.task}?" %>
  </div>
  <%= f.submit "Change task"%>
<% end %>
